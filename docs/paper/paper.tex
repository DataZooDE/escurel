%% Two Dualities: Skills and Instances, Events and Callbacks
%%
%% PVLDB experience-track style. Built with `make` in this directory; the
%% acmart class is vendored under cls/ because it is not in every distro's
%% base texlive install (see README.md).
%%
%% STATUS: all eight sections are drafted. Every numeral traceable to a
%% harness, a named test, or the predecessor's verification tree is set.
%% A number that is not yet measured is written \todonum{} and renders in
%% red, so the gaps are visible in the PDF and not only in the source.
%% Numbers inherited from the 430-line Python predecessor carry a
%% superscript p and are never presented as results of this system.

\documentclass[sigconf, nonacm]{acmart}

\usepackage{booktabs}
\usepackage{balance}
\usepackage{xcolor}
\usepackage{tikz}
\usetikzlibrary{positioning, arrows.meta}

\settopmatter{printacmref=false}
\renewcommand\footnotetextcopyrightpermission[1]{}
\pagestyle{plain}

%% acmart draws a ZapfDingbats glyph in the title block. That font ships in
%% texlive-fontsrecommended, which is not installed everywhere, and its
%% absence is a hard stop rather than a warning. The glyph is decorative, so
%% we drop it rather than make the build depend on an optional package.
\AtBeginDocument{\providecommand{\ding}[1]{}\renewcommand{\ding}[1]{}}

%% An unmeasured number. Deliberately loud: the paper must not ship with one.
\newcommand{\todonum}[1]{\textcolor{red}{\textbf{[?~#1]}}}
%% A prototype-era number, inherited from the predecessor draft. Never a
%% result of the system described here.
\newcommand{\pn}[1]{#1\textsuperscript{\textsf{p}}}
%% A cross-reference to the guarantee register in Section 5.
\newcommand{\expref}[1]{\textsf{E-#1}}

\begin{document}

\title{Escurel: A Typed Knowledge Base for Agents}
\subtitle{Skills and Instances, Events and Callbacks, and One Referent Space
over Five Storage Substrates}

\author{Joachim Rosskopf}
\affiliation{%
  \institution{DataZoo GmbH}
  \country{Germany}
}
\email{jr@data-zoo.de}

\begin{abstract}
We present \texttt{escurel}, a knowledge base built for agents rather than
for people, and report what ten weeks and 101{,}500 lines of Rust taught us
about it. The system rests on two dualities. A skill page such as
\texttt{customer} is both the schema its instances must satisfy and the
prose an agent reads to learn what a customer is for, and an instance such
as \texttt{customer::acme-corp} is both a row of that type and a markdown
file somebody edits in a text editor. The second duality is temporal: events
are immutable facts that land in an inbox, and the agent run that folds one
into instance state takes its instructions from that event's own skill page.
The first duality is what bounds context cost: because discovery happens at
the level
of types, an agent's cold-start cost tracks the number of skills, which
people author and which therefore stops growing, rather than the number
of instances, which machines generate. A corollary follows that we did not
anticipate and that turned out to matter more than either duality alone.
Since a skill declares its own backend, an instance's storage substrate is a
property of its \emph{type}. The same \texttt{resolve}, \texttt{expand}
and \texttt{neighbours} calls reach native markdown, a read-only SQL view
over Postgres, an extracted PDF, a live REST endpoint and an upstream MCP
server; only similarity search stops at the three substrates that are
materialised, which is the one place the abstraction is visible from the
agent's side. We give the design, a register of eighteen safety and
semantic guarantees in which every row names an integration test that
actually runs, and an evaluation protocol covering context cost at five
corpus scales, retrieval against flat retrieval over identical bytes, and
the cost of each substrate. We also report what went
wrong: the default embedder our own specification pins cannot be loaded by
our own runtime, and lexical retrieval collapsed to \pn{0.350} nDCG on a
duplicate-stress corpus where vector retrieval held at \pn{0.933}.
\end{abstract}

\maketitle

\renewcommand{\thefootnote}{}
\footnotetext{Numbers marked \pn{x} are prototype-era: they come from the
430-line Python predecessor~\cite{predecessor} and its verification tree,
not from the system described here. Numbers written \todonum{like this} are
specified in Section~\ref{sec:eval} but not yet measured.}
\renewcommand{\thefootnote}{\arabic{footnote}}

%% =====================================================================
\section{Introduction}

\textbf{Agent memory is a context problem, not a storage problem.}
Storing a million facts is not hard and has not been hard for decades. What
is hard is getting the right handful of them into a bounded context window,
over and over, cheaply enough that an agent can afford to look. Every
retrieval a language model performs is paid for twice, in tokens and in
turns, and both prices are set by how much the knowledge base makes the
agent read before it can act. Flat vector memory --- chunk the corpus, embed
the chunks, retrieve by cosine similarity --- answers exactly one question,
``what text resembles this text,'' and charges a document body for every
answer. It has no notion of what kind of thing it just returned.

\textbf{Two dualities.}
We argue that two dualities, and one corollary of the first, are what let a
knowledge base grow without the agent reading more to use it. The first is static: a \emph{skill} is
simultaneously a type (the frontmatter schema a class of pages must satisfy)
and a prompt (prose that tells a model what this kind of thing is for), and
an \emph{instance} is simultaneously a row of that type and a markdown
document a person can edit in any text editor. The second is dynamic: an
\emph{event} is an immutable fact that lands in an inbox, and a
\emph{callback} is an agent-harness run that projects it into instance
state, taking its instructions from a skill page.
Neither works alone. Skills without events give you a wiki that rots; events
without skills give you an append-only log nobody can query.

\textbf{The corollary: not everything is markdown, and it never will be.}
An organisation's knowledge does not live in one place. Some of it is prose,
some is a Postgres table, some is a PDF nobody has read since it was signed,
and some exists only behind a CRM's REST API. Because a skill declares its
own \texttt{backend:}, the substrate an instance's data lives on is a
property of the instance's \emph{type} rather than of the referent space.
One skill's instances are markdown; another's are rows of a read-only SQL
view; another's are extracted document chunks; another's are fetched live
from a REST endpoint on every read, and accept writes back upstream. The
agent resolves, expands and traverses all of them with the same calls, and
nothing in its prompt branches on where the bytes came from. There is one
exception, and we would rather name it here than bury it: the two live
substrates are never indexed, so similarity search does not reach them.

\textbf{What we built, and what it cost.}
\texttt{escurel} is 23 Rust crates and roughly 101{,}500 lines, with 172
integration-test files, written over ten weeks. Three applications run on
it and drove most of its surface: a knowledge explorer, a document and chat
workspace, and a report renderer. It deploys as a pet rather than cattle,
pinned to one host and stopped before it is replaced, because a DuckDB file
has exactly one writer and we would rather make that visible in the
deployment than paper over it in the code. Storage
is one DuckDB file per tenant carrying relational metadata, an HNSW vector
index, a BM25 index and a CRDT operation log, beside a \texttt{pages/}
markdown directory that remains the canonical source of truth. Every agent
that writes to the corpus runs in a separate process, so the store has no
background writer of its own. That constraint cost us a component and
bought us the ability to reason about the store without reasoning about the
loop.

\textbf{Contributions.}
\begin{itemize}
\item The two-duality model and its substrate corollary, with the mechanism
  by which typing, not disclosure discipline alone, is what bounds an
  agent's context cost (Sections~\ref{sec:background}--\ref{sec:model}).
\item A design that realises it: typed wikilinks validated at index time,
  one embedded file carrying all five index concerns, five instance backends
  behind one referent space, and a reactive projection loop whose reducer
  generalises
  from one-hop cascades to fan-out workflows (Section~\ref{sec:design}).
\item A guarantee register comparing \texttt{escurel} against flat vector
  memory in which every row names an integration test that actually runs
  (including the row where the test we had was asserting the wrong thing)
  (Section~\ref{sec:guarantees}).
\item An evaluation protocol, specified to the level of the harness
  invocation, covering context cost at five corpus scales, retrieval against
  flat retrieval over identical bytes, and a five-substrate cost profile
  (Section~\ref{sec:eval}).
\item Negative results, reported because each was the natural thing to
  believe (Section~\ref{sec:eval}, Section~\ref{sec:limits}).
\end{itemize}

\textbf{What this draft can and cannot defend.}
The design, the guarantee register and the failures we report are complete.
The measurements are not. Section~\ref{sec:eval} specifies each experiment
down to its harness invocation, and every cell we have not run prints in red
instead of being described in prose. On the evidence here we can defend an
architecture and the discipline used to verify it. The claim that this bounds
an agent's context cost at scale rests on Section~\ref{sec:eval}'s first
experiment, which is specified and not yet run. We would rather say so than
let the surrounding argument imply otherwise.

%% =====================================================================
\section{Background: What Flat Memory Costs}
\label{sec:background}

\textbf{The cost model.}
An agent's cost on a task is the number of tokens it carries in context
multiplied by the number of turns it takes. Both are functions of one design
choice: how much the knowledge base makes it read before it can decide what
to read next. We call the metadata an agent loads at cold start
\emph{Tier~1}, and the bodies it later chooses to pull \emph{Tier~2}. The
distinction is Anthropic's~\cite{agentskills}, applied to procedural
knowledge; the question this paper asks is what it takes to apply it to
episodic knowledge, which is where the volume actually is.

\textbf{Flat vector memory has no Tier~1.}
Without types there is nothing to enumerate. There is no set of a hundred
things an agent can hold in mind while it decides which of a million things
to fetch, so every question becomes a similarity query over the whole corpus
and every answer arrives as a body. Table~\ref{tab:costmodel} gives the
arithmetic that follows, measured on the predecessor's
corpora~\cite{predecessor}. The numbers are prototype-era and the ratio is
what matters: at ten thousand pages, reading metadata only costs three to
five thousand tokens, and reading everything costs four hundred to six
hundred thousand.

\begin{table}[t]
\caption{What a cold start costs, by disclosure strategy. Prototype-era
measurements over the predecessor's corpora, \texttt{tiktoken cl100k\_base}.
The working budget is a nominal 30k tokens.}
\label{tab:costmodel}
\small
\begin{tabular}{lrr}
\toprule
strategy & 10k pages & 100k pages \\
\midrule
every page, full content     & \pn{400--600k} & \pn{4--6M} \\
every page, metadata only    & \pn{3.2--4.8k} & \pn{8--12k} \\
every \emph{skill}, metadata only & \pn{189}  & \pn{189} \\
\bottomrule
\end{tabular}
\end{table}

The third row is the one that motivates the paper. It does not grow, because
what it enumerates is not the corpus.

\textbf{Flat memory also has exactly one substrate.}
This gets less attention than the token arithmetic and causes at least as
much trouble in practice. A flat vector store can reason only about text it
has already ingested, so every table, every document and every record behind
an API must first be copied in and chunked. The copy is stale the moment it
lands, the pipeline that refreshes it is a component somebody has to run,
and the parts of the business that change fastest are usually the parts that
are most expensive to keep copied. Section~\ref{sec:model} argues that this
is not an ingestion problem to be solved with better pipelines but a typing
problem, and that once types exist it mostly dissolves.

\textbf{``Use a bigger window'' is not an answer.}
Long-context models make the naive strategy possible at small scale and
still not advisable: cost and latency grow with the window, and retrieval
accuracy degrades for material in the middle of it~\cite{lostmiddle}. More
to the point, the strategy has no stable operating point. A corpus that
fits today does not fit next quarter, and the failure is silent.

\textbf{What a write costs.}
Flat memory's write path is ``embed and append.'' Nothing validates the new
material against anything, nothing supersedes what it replaces, and nothing
links it to what it concerns. The corpus therefore accumulates
contradictions at exactly the rate it accumulates content, and the retrieval
layer, which ranks by similarity, is structurally unable to prefer the
correct one of two similar answers.

\textbf{And then it goes stale.}
The complement is just as bad. A typed knowledge base with no ingestion loop
is a wiki: correct on the day it was written, quietly wrong six weeks later,
and trusted for far longer than that. Any model of agent memory that stops
at the data model has described half a system. That is why this paper has
two dualities in it and not one.

%% =====================================================================
\section{Two Dualities and a Corollary}
\label{sec:model}

\subsection{Skill \texorpdfstring{$\leftrightarrow$}{<->} Instance}

The observation the system rests on is small and, once made, hard to unsee:
an Agent-Skill page and a personal-wiki note are the same artefact. Both are
markdown with frontmatter, an identifier and a description. The only
difference is that one is read as instructions and the other as data. So we
let an instance page declare which skill it conforms to, and the skill
becomes that instance's type.

\begin{figure}[t]
\centering
\begin{tikzpicture}[
  font=\small,
  box/.style={draw, rounded corners=2pt, align=center, inner sep=4pt,
              minimum height=7mm},
  sub/.style={draw, dashed, rounded corners=2pt, align=center,
              inner sep=3pt, font=\footnotesize},
  ar/.style={-{Latex[length=2mm]}}
]
\node[box] (skill) {\textbf{skill}\\\footnotesize schema $+$ prompt};
\node[box, below=9mm of skill] (inst) {\textbf{instance}\\\footnotesize row $+$ document};
\node[box, right=16mm of skill] (event) {\textbf{event}\\\footnotesize immutable fact};
\node[box, right=16mm of inst] (cb) {\textbf{callback}\\\footnotesize agent run};

\draw[ar] (skill) -- node[left, font=\footnotesize] {\textcircled{\tiny 1} types} (inst);
\draw[ar] (event) -- node[right, font=\footnotesize] {\textcircled{\tiny 3}} (cb);
\draw[ar] (skill) -- node[above, font=\footnotesize, pos=0.5] {\textcircled{\tiny 2} instructs} (event);
\draw[ar] (cb) -- node[below, font=\footnotesize] {\textcircled{\tiny 4} projects} (inst);

\node[sub, below=7mm of inst] (subs)
  {\textcircled{\tiny 5}\; markdown \;$\vert$\; sql\_view \;$\vert$\; document
   \;$\vert$\; openapi \;$\vert$\; mcp};
\draw[ar] (inst) -- (subs);
\draw[ar, dashed] (skill.west) -- ++(-6mm,0) |- node[left, font=\footnotesize, pos=0.25] {\textcircled{\tiny 6}} (subs.west);
\end{tikzpicture}
\caption{The model. A skill types its instances \textcircled{\tiny 1} and
instructs the callback that handles an event of that kind
\textcircled{\tiny 2}; an event fires a callback \textcircled{\tiny 3},
which projects it into instance state \textcircled{\tiny 4}. An instance's
data may sit on any of five substrates \textcircled{\tiny 5}, chosen by the
\emph{skill} \textcircled{\tiny 6} and never by the caller.}
\label{fig:model}
\end{figure}

\textbf{The type is carried by the link, not inferred from it.}
References take the form \texttt{[[skill::id]]} and are validated when the
page is indexed. Four checks run on every typed link: the skill page exists,
the target page exists, the target's own \texttt{skill:} frontmatter field
equals the link's skill segment, and any named anchor is present on the
target. A link that fails one is reported, never silently dropped. The skill
segment is then stored as a column on the link row, which makes ``who cites
any decision record'' an indexed relational query rather than a graph walk.

\textbf{Why this bounds context cost.}
Discovery enumerates skills. Skills are written by people. They are the
vocabulary of a domain, not its contents, so their count is bounded by
human attention rather than by ingestion rate. A tenant that ingests a
million meeting notes still has one \texttt{meeting} skill. This is the
whole mechanism, and it is worth being precise about what it does and does
not claim: progressive disclosure bounds cost \emph{only because} typing
gives it something to disclose about. Disclosure discipline applied to an
untyped corpus has no level of description to stop at.

Concretely: an agent asked what changed with a customer last quarter begins
by listing skills, which returns on the order of a hundred identifier and
description pairs and no bodies at all. It reads \texttt{meeting} and
\texttt{decision-record} out of those descriptions, issues one
\texttt{list\_instances} call filtered by skill and date range, and expands
the two or three pages it actually needs. The other several hundred thousand
instances cost it nothing, because no step in that sequence has a size that
depends on how many there are.

\textbf{Links carry anchors and versions too.}
A reference splits on \texttt{|} (alias), \texttt{\#} (anchor), \texttt{@}
(version) and \texttt{::} (skill), in that order, giving six usable forms
from the bare \texttt{[[skill]]} up to the full
\texttt{[[skill::id\#anchor@version}\-\texttt{|alias]]}. An anchor addresses a block
within a page; the version pins a snapshot. Typing also closed a real class
of ambiguity: in the untyped predecessor, \texttt{[[Anna Mueller]]} and
\texttt{[[anna-mueller]]} resolved to two different nodes, because nothing
said what a canonical form was.

\subsection{The Type Declares Where the Value Lives}
\label{sec:corollary}

Skills carry a \texttt{backend:} block, and the skill is where it is
carried: never the instance, never the caller. Five substrates exist today:

\begin{itemize}
\item \textbf{\texttt{markdown}} --- the default. Writable at block
  granularity through a CRDT session, indexed, searchable.
\item \textbf{\texttt{sql\_view}} --- a read-only DuckDB \texttt{VIEW} over
  an external relational source (Postgres, MySQL, SQLite, an ERP connector,
  or directories of JSON or Parquet). Materialised, so it is searchable.
\item \textbf{\texttt{document}} --- an uploaded PDF, DOCX, PPTX, XLSX or
  text file, extracted, chunked and embedded into one page with blocks.
\item \textbf{\texttt{openapi}} --- a live REST endpoint. Nothing is stored:
  the body is fetched on every \texttt{expand}, and writes are forwarded
  upstream.
\item \textbf{\texttt{mcp}} --- a live upstream MCP~\cite{mcp} server, read
  as a tool call or a resource, with the same write-back path.
\end{itemize}

\textbf{One invariant makes this cheap.}
Every external instance keeps a markdown overlay page. The page \emph{is}
the instance as far as the referent space is concerned: identity, links,
access control and history all reuse machinery that already existed. A
\texttt{backend\_ref} block binds the page to the external data. All the novelty
is confined to where the body comes from. That is why adding a backend
required no change to the dispatcher, no change to the wire format and no
new tool for the reading agent: \texttt{resolve}, \texttt{expand},
\texttt{neighbours}, \texttt{list\_*} and \texttt{search} route through the
backend transparently.

\textbf{The division that does show through is materialisation.}
\texttt{sql\_view} and \texttt{document} are materialised and therefore
indexed and searchable. The two live proxies are not: their data never
enters DuckDB, so they report \texttt{search: "none"} and an agent cannot
find them by similarity, only by link or by the overlay page's own
metadata. We regard this as an honest limit rather than a defect
(Section~\ref{sec:limits}), but it is the one place where the substrate is
visible from the agent's side, and we say so rather than claiming a
transparency we do not have.

\textbf{Time travel is expressed in the same syntax.}
A link may pin a version: \texttt{[[table::customers.churn@v14]]} resolves
against snapshot 14 of an attached DuckLake~\cite{ducklake} catalog, and
\texttt{@2026-04-12} pins by date. For markdown instances the segment is
accepted and ignored. One notation covers ``the current state of a customer
record in our CRM'' and ``the churn table as it stood the morning the
decision was taken.''

\subsection{Event \texorpdfstring{$\leftrightarrow$}{<->} Callback}

An event is an instance of an event-typed skill --- \texttt{meeting},
\texttt{email}, \texttt{incident}, \texttt{commit} --- carrying an
\texttt{at:} timestamp and typed links to whatever it concerns. Events are
immutable by convention: a correction is a new event that cites the old one,
not an edit. The inbox is simply the suffix of the log that has not been
projected yet.

\textbf{The callback's instructions are a skill page.}
When an event arrives, a runner triggers an agent harness whose task
description \emph{is} the event's \texttt{label\_skill} page and whose
toolset is the ordinary MCP endpoint. There is no bespoke per-harness client
and no prompt template maintained outside the corpus. The instructions for
processing meetings live in the \texttt{meeting} skill, where a human can
read, review and edit them, and where they are versioned with everything
else.

\textbf{State lives in the knowledge base, not in a script.}
This is the inversion that makes the loop recoverable. A workflow engine
that holds progress in local variables loses it when the process dies. Here,
progress is events and instances, which are durable, queryable and
replayable, so a crashed run resumes by re-reading what it already wrote.

\subsection{The Cross Product}

The two dualities are not independent contributions that happen to share a
repository. Each supplies something the other needs.

The skill-as-schema is what the callback's write is validated against;
the skill-as-prompt is what drives the callback in the first place. The
event is the instance's provenance; the callback is how the instance
acquires a current value at all. And the corollary is what lets the loop
reach past the corpus: because an instance may be a live REST record, a
callback can fold an event into a system \texttt{escurel} does not own,
writing back through \texttt{write\_instance} rather than into a copy that
will drift.

Drop the corollary and the other two still work, but only over material the
system has already imported, which is where most retrieval-augmented
architectures sit and why so much of their engineering turns out to be
pipeline engineering.

%% =====================================================================
\section{Design and Implementation}
\label{sec:design}

\subsection{One File, Five Index Concerns}

Each tenant is a markdown directory beside a single DuckDB~\cite{duckdb}
file. That file carries relational metadata and the typed link graph; a
table of blocks, one row per markdown block, whose vector column is indexed
by HNSW through the \texttt{vss} extension and whose body column is indexed
by BM25 through \texttt{fts}; the CRDT operation log; and an attach point
for read-only lakehouse catalogs. A filtered vector search is
therefore one SQL statement, because the filter columns are denormalised
onto \texttt{blocks} beside the vector.

The predecessor split this across LanceDB for retrieval and DuckDB for
relational state, bridged through Arrow, with CRDT operations in sidecar
files. Lance won the original benchmark: \pn{0.975} nDCG, tying exact
KNN, at roughly half the p50 latency and a sixth of the disk. We collapsed
it anyway, accepting a measurable latency increase in exchange for deleting
a storage engine, a cross-store consistency boundary, a sidecar atomicity
argument and a three-way audit. The thresholds at which we would have
reverted were declared in advance; Section~\ref{sec:eval} reports where we
stand against them, which is not where we would like.

The \texttt{pages/} directory stays canonical. Everything in the database is
derivable from it, which means \texttt{git diff} still works on a corpus,
Obsidian still edits it, and recovery is a rebuild rather than a restore.

\subsection{Typing at Index Time}

The wikilink parser is a regular expression rather than a markdown AST
walk. Markdown parsers split \texttt{[[} across nodes
in ways that depend on the surrounding context, so the bracket that opens a
reference and the one that closes it need not land in the same node. We
evaluated the AST path and abandoned it before writing the indexer.
The four validation checks run in the same pass, and their output is the
same issue list that the \texttt{validate} tool returns on a dry run, so
an authoring agent gets exactly the feedback the indexer would give it,
before it writes.

\subsection{One Referent Space, Five Substrates}
\label{sec:substrates}

The corollary of Section~\ref{sec:corollary} is where a security reviewer
should look first, because it is the part of the system that makes outbound
network calls on behalf of tenant-authored content. Five invariants keep it
safe, and each is a test rather than an intention.

\textbf{Secrets never enter the corpus.} A \texttt{sql\_view} names a
credential registered out of band by an administrator and realised as a
DuckDB secret; the listing tool returns names only. Markdown never contains
a password, so exporting a tenant never exports one.

\textbf{There is no SSRF surface.} A remote skill names an
\emph{administrator-registered endpoint}, not a URL. Tenant markdown cannot
cause the server to fetch an arbitrary host, and a skill whose declared kind
disagrees with the kind its endpoint was registered under fails closed
rather than guessing.

\textbf{A failed read never fabricates a body.} If a live source times out
or errors, \texttt{expand} returns the overlay page plus an explicit issue
marker, and \expref{15} is that path under an unreachable host. A knowledge
base that answers confidently from a source it could not reach is worse than
one that is down.

\textbf{Writes are value-bound.} Remote write-back fills path and body
templates from scalar payload fields; an exact leaf keeps its JSON type, and
a placeholder that cannot be resolved aborts the call rather than sending a
literal brace to an upstream system.

\textbf{Schema drift is detected, not tolerated.} A \texttt{sql\_view}
captures a fingerprint of its source schema at creation and revalidates it
on attach, marking the binding degraded when the source has moved
underneath it.

\subsection{The Write Path}

Live editing is a CRDT operation stream over Loro~\cite{loro}, persisted
transactionally with the relational changes so that a commit is atomic
across pages, links, blocks, indexes and operations together. A whole-page
\texttt{update\_page} fallback exists for clients that cannot hold a
session. Writes take a per-tenant write lock; reads do not take it at all.
Markdown is written with write-then-rename, and only after the database
commit succeeds, so a crash leaves the two reconcilable in one direction,
which is the direction the rebuild runs.

The non-markdown backends are read-only on this path by construction.
Remote write-back is a separate, explicitly named tool, gated by the target
instance's own access control. We considered making \texttt{update\_page}
transparently write through to a REST endpoint and rejected it: a page edit
and an API call have different failure modes, and papering over that with a
shared verb would have made both harder to reason about.

\subsection{All Automation Outside the Gateway}

The knowledge base schedules no work against tenant content. A separate
runner process subscribes
to an outbound webhook and also polls the inbox (the poller is the
self-healing path for a webhook that was missed), and both converge on one
bounded per-tenant queue. A runner-local durable ledger, deliberately not in
the tenant store, holds one row per run and is the basis of every loop
control: idempotency, deduplication, depth and budget caps, cycle detection.

One periodic task does run inside the gateway, and naming it is the point: a
reader replica polls for newly published snapshots and hot-swaps its index.
It only ever reads, and it exists because a read replica that never notices
a new snapshot is not a replica.

We were tempted more than once to move the rest inside as well. Keeping it
out means the store has no background writer of its own, so ``what is in the
knowledge base'' is a question about the knowledge base and not about a
scheduler's state.

\subsection{One Loop at Two Settings}

The cascade emitter is already a reducer over a run's event log, just a
degenerate one: it emits at most one follow-on event per confirmed
cross-skill write, with no join. A multi-phase workflow is the same reducer
with three knobs turned up: an explicit plan read from a workflow skill,
fan-out width greater than one, and a quorum barrier where a phase waits for
$k$ sibling completions.

Fan-out orchestration is currently being built as a second execution model
beside the reactive one, and it does not need to be. Running a workflow is
itself an inbox event; the only thing separating it from an externally
captured one is who captured it.

\subsection{Packs: Shipping Types, and Their Bindings}

Skills are distributable. A pack is a versioned, read-only layer of pages
that a tenant subscribes to; local pages shadow base pages of the same name,
promotion back into the base layer passes a curator gate, and a pinned
version never moves on its own. With the corollary in play, a pack ships
more than a schema: a CRM pack carries skills whose instances resolve
against \emph{the subscriber's} registered endpoint. The type library and
the integration arrive together, and the subscriber supplies only the
credential.

\subsection{What We Refuse}

No raw SQL on the agent surface, no direct vector access, no cross-tenant
operation, no auto-provisioning on first request, and no raw URL in tenant
markdown. Each refusal removes a capability that would have been convenient
and an attack or corruption path that would have been permanent.

%% =====================================================================
\section{Guarantees and Their Verification}
\label{sec:guarantees}

A knowledge base is a contract. Tests passing is not the same as a contract
being stated, and a contract nobody wrote down is one the next component
will violate. Table~\ref{tab:guarantees} states what \texttt{escurel}
promises, what a flat vector store promises for comparison, and for each row
the test that verifies it. Every named test runs in the ordinary workspace
test command; none is behind an ignore attribute, because a claim whose test
does not run is not a claim.

\begin{table*}[t]
\caption{The guarantee register. Every row names an integration test that
runs in \texttt{cargo test --workspace --all-targets}. A flat vector store
over the same corpus provides none of these except a durable append
(\expref{5}): it has no typed reference to resolve, no schema to validate
against, no session to converge, and no binding to degrade.}
\label{tab:guarantees}
\footnotesize
\setlength{\tabcolsep}{4pt}
\begin{tabular}{lll}
\toprule
 & promise & verified by \\
\midrule
\expref{1} & \texttt{resolve} of a valid typed link is deterministic and total & \texttt{resolve\_typed\_wikilink\_to\_known\_page} \\
\expref{2} & a link failing any of the four checks is reported, never dropped & \texttt{validate\_instance\_with\_unknown\_declared\_skill\_errors} \\
\expref{3} & anchors and versions survive resolution unchanged & \texttt{resolve\_typed\_wikilink\_with\_anchor\_and\_version\_keeps\_segments} \\
\expref{4} & concurrent CRDT sessions on one page converge & \texttt{concurrent\_apply\_ops\_serialise\_through\_actor} \\
\expref{5} & a mid-write crash rolls back every table together & \texttt{mid\_write\_panic\_rolls\_back\_all\_tables} \\
\expref{6} & the index is reproducible from markdown alone & \texttt{node\_loss\_then\_rebuild\_from\_markdown\_reproduces\_index} \\
\expref{7} & a stale base version is rejected rather than silently merged & \texttt{version\_advances\_and\_stale\_base\_version\_conflicts} \\
\expref{8} & one event yields exactly one terminal run, however delivered & \texttt{same\_event\_twice\_yields\_exactly\_one\_terminal\_run} \\
\expref{9} & a cascade terminates on depth or cycle, and is dead-lettered & \texttt{cascade\_cycle\_is\_dead\_lettered\_at\_depth\_or\_cycle} \\
\expref{10} & a workflow phase completes only when its barrier quorum is met & \texttt{verify\_barrier\_runs\_to\_completion\_via\_echo} \\
\expref{11} & every non-markdown backend refuses page-level writes & \texttt{update\_page\_on\_sql\_instance\_rejected\_backend\_read\_only} \\
\expref{12} & a subscribed pack's base pages cannot be written or forged & \texttt{update\_page\_cannot\_fabricate\_a\_base\_layer\_page} \\
\expref{13} & credentials never appear in the markdown corpus & \texttt{secret\_is\_server\_side\_only\_never\_in\_markdown\_corpus} \\
\expref{14} & a remote read fails closed on a kind mismatch & \texttt{remote\_read\_fails\_closed\_on\_endpoint\_protocol\_mismatch} \\
\expref{15} & an unreachable endpoint degrades the read, never fabricates it & \texttt{openapi\_unreachable\_endpoint\_degrades\_read\_and\_probe} \\
\expref{16} & an unresolvable write placeholder aborts the upstream call & \texttt{mcp\_resource\_read\_fails\_closed\_on\_unresolved\_placeholder} \\
\expref{17} & source schema drift marks the binding degraded & \texttt{schema\_drift\_marks\_binding\_degraded} \\
\expref{18} & a token minted for another tenant is refused, for every role & \texttt{foreign\_tenant\_rejected\_and\_served\_tenant\_quota\_enforced} \\
\bottomrule
\end{tabular}
\end{table*}

\textbf{On \expref{18}, which is the row that changed shape.}
Our specification describes a tenant manager holding a bounded cache of
per-tenant handles, each with its own pool and write lock. It is not
implemented. The binary wires one shared indexer and one served tenant, so
the deployed isolation boundary is the process, not a data structure inside
it: one instance, one tenant. That suits a per-tenant deployment model well
enough, but it is not what the specification says.

We learned this the way these things are usually learned. Authentication
resolved a tenant from the token's claim and the data path used the served
tenant, and for some weeks nobody compared the two. A validly signed token
for tenant B, presented to tenant A's gateway, was accepted, had its quota
debited against B, and then read and wrote A's corpus. What makes it worth
recounting is that a test had encoded the bug as intended behaviour: a
foreign token succeeding was being used to demonstrate that quota buckets
were per-tenant. The gateway now rejects a mismatch with 403 for every role,
including administrators, and \expref{18} is that test rewritten. A
guarantee register is worth having mostly because it forces this comparison;
its characteristic failure is becoming a place where aspirations get
recorded as properties.

\textbf{Remote backends are tested against real servers.}
The tests behind \expref{14}--\expref{16} start actual HTTP services and an
actual upstream MCP server in-process, with authentication, unauthorised
responses and unreachable hosts among the cases. This follows a project rule
that a test which mocks the boundary it exists to cover is not finished. It
is more work per test and it is the reason we trust these five rows more
than we trust our own architecture diagram.

%% =====================================================================
\section{Evaluation}
\label{sec:eval}

\subsection{Setup and Protocol}

Retrieval runs use a 768-dimensional BERT-family encoder,
\texttt{BAAI/bge-base-en-v1.5}, and a cross-encoder reranker from the same
family. Two constraints pin that choice and are worth stating because one of
them is a defect: the block vector column is fixed at 768 dimensions, and
our Candle-based loader supports BERT architectures only. Datasets are in
BEIR format~\cite{beir}, the corpus identifier is used verbatim as the page
identifier so that relevance judgements compare directly against hits, and
the corpus is embedded once and reused across configurations.

Three rules govern every number below. Legs that are compared are measured
in one session on one machine with one warm model cache, because a
cross-session ratio is an artefact. No claim about agent behaviour is made
without a real language-model run; counts produced by a tokeniser alone are
labelled as such. And every unmeasured cell is printed \todonum{like this},
so that a gap is visible in the document rather than only in its source.

\subsection{Context Cost Against Corpus Size}

The headline experiment. At instance counts spanning $10^2$ to $10^6$, over
identical corpus bytes, we compare \texttt{escurel} against flat retrieval
and against putting the whole corpus in the window, measuring Tier-1 tokens
at cold start, tokens and tool calls to a first correct answer, end-to-end
tokens over a ten-task session, and success rate. Correctness is graded
against a pre-registered answer key by exact match on a named entity or a
numeric field, not by a model judge, over ten runs per cell.

The prototype's real-model run reached a correct answer in \pn{1--2} tool
calls at \pn{100\%} success and \pn{12.4$\times$} lower token cost than a
flat baseline, and its Tier-1 cost held at \pn{189} tokens across the full
six orders of magnitude. Reproducing that on the real system at five scales
is measurement \todonum{Table 3}, and it is the single result on which the
paper's central claim stands or falls.

One scope note we insist on. The invariant is a \emph{Tier-1} invariant.
Total context still grows with how many bodies an agent chooses to expand,
and a paper that reports the first number without the second is selling
something. Both belong in the table.

\subsection{Retrieval Against Flat Retrieval Over Identical Bytes}

Five configurations over the same corpus: flat retrieval with no types and
no filter; single-pass typed retrieval; a two-pass variant with a truncated
coarse dimension; a reranked variant; and both together. We report nDCG@10,
recall@100, MRR@10, p50 and p95 latency and sustained throughput. Four of
the five run today. The flat baseline is the new work, and it has to be a
tuned one, so we sweep $k$ and report the sweep beside the result rather
than only the best cell. Result: \todonum{Table 4}.

\subsection{Is It the Type, or Just a Filter?}

A reviewer will ask whether the win is typing or merely metadata filtering,
and the honest answer requires an ablation: skill filter on and off, crossed
with typed link expansion on and off, over the same queries
(\todonum{Table 5}).

We can already report the direction of one cost. The predecessor measured
description-only indexing at \pn{83.3\%} top-1 against a full-content
baseline of \pn{96.7\%}. Disclosure buys tokens by giving up accuracy at the
discovery step and recovering it in a second call. That trade is the
mechanism, not a side effect, and it is why Section~\ref{sec:limits} names
corpora where it is a bad trade.

\subsection{Does the Skill Count Stay Bounded?}

This is the experiment that could falsify the central claim, so it is worth
more than the ones that could confirm it. We plot skill count and instance
count against time over the real corpus history of three tenants
(\todonum{Figure 2}). If skills grow linearly with instances, the mechanism
does not work and the paper says so. Ten weeks of one organisation's data is
a regime, not a scaling law, and we will report it as such.

\subsection{The Projection Loop, and Replay}

Over a seeded stream of events across several skills we measure end-to-end
throughput, agent runs per event, cascade depth distribution,
time-to-quiescence, and the share of cascades stopped by each loop control
(\todonum{Table 6}). Measurement uses an echo harness so that the number
describes the loop rather than a language model, with one real-harness run
for the constant factor, and repeats at fan-out widths of 1, 4 and 16 to
show that generalising the reducer costs what the width says and nothing
structural.

Replay is the sharper test. We kill the runner at fifty pseudo-random points
in a cascade of known shape, restart, and require the final instance state to
be byte-identical to an uninterrupted run (\todonum{convergence rate}). We
would rather report one divergence class found and fixed than fifty clean
runs, and we will say which we got.

\subsection{Five Substrates, One Call Shape}

The experiment that turns the corollary into a result. One fixture tenant
carries five skills describing the same logical entity, a customer,
one per backend, against a real Postgres instance, a real uploaded PDF, a
real REST service and a real upstream MCP server. The same task runs against
each. Table~\ref{tab:substrates} gives the shape; the cells are measurement.

\begin{table}[t]
\caption{The same task against one entity on five substrates. The claim
under test is that the first row is identical across all five columns.}
\label{tab:substrates}
\footnotesize
\setlength{\tabcolsep}{3.5pt}
\begin{tabular}{lccccc}
\toprule
 & markdown & sql & doc & rest & mcp \\
\midrule
tool calls to answer   & \todonum{} & \todonum{} & \todonum{} & \todonum{} & \todonum{} \\
tokens to answer       & \todonum{} & \todonum{} & \todonum{} & \todonum{} & \todonum{} \\
\texttt{expand} p50 (ms) & \todonum{} & \todonum{} & \todonum{} & \todonum{} & \todonum{} \\
\texttt{expand} p95 (ms) & \todonum{} & \todonum{} & \todonum{} & \todonum{} & \todonum{} \\
indexed / searchable   & yes & yes & yes & no & no \\
writable via           & page & --- & --- & \multicolumn{2}{c}{instance} \\
source down            & n/a & degr. & n/a & degr. & degr. \\
\bottomrule
\end{tabular}
\end{table}

Two things are being measured, and only one of them is a performance
number. The first row is the corollary: if the agent's call sequence differs
per substrate, the abstraction leaks and we report where. The latency rows
are the honest cost: materialised backends answer from a local file, and
live proxies pay a network round trip inside a read. They feed
Section~\ref{sec:limits} directly.

\subsection{What the Storage Collapse Cost}

We declared thresholds in advance before merging retrieval and relational
state into one file: nDCG at or above 0.95 on a 460-block corpus and 0.90 on
a 10,120-block stress corpus, p50 vector search within 15\,ms, p95 filtered
search within 50\,ms at 100k instances. The Lance baselines were \pn{0.975},
\pn{0.933}, \pn{4.3}\,ms and \pn{18.71}\,ms.

We cannot report the comparison. The labelled corpus those thresholds were
declared against is not in this repository, and it is not in the research
tree either; it was never versioned with the code that depends on it. The
gate has been open since May, and reopening it means rebuilding the corpus
and its relevance judgements from scratch, which is a larger job than the
measurement it was meant to gate. We record it as a finding about process
rather than about DuckDB: a pre-deployment gate whose fixture is not
versioned alongside the code is a gate that will still be open when you
deploy, and ours was.

\subsection{Two Negative Results}

\textbf{Lexical retrieval collapsed where vector retrieval did not.}
On a 10,120-block stress corpus built by expanding 460 originals into
synonym mutants, vector retrieval held at \pn{0.933} nDCG, four points below
its small-corpus baseline. Lexical retrieval with a default tokeniser fell
to \pn{0.350}, ranking mutants above originals on 17 of 30 queries. Part of
this is an artefact of the stress construction, since real corpora do not
contain twenty near-identical copies of every page, but we had assumed BM25
degrades gracefully under near-duplication, and it does not.

\textbf{The default embedder our specification pins cannot be loaded by our
own runtime.} The configuration names EmbeddingGemma. The Candle transformer
library has no Gemma-3 sentence-encoder path, so it will not load, and the
actual default is a BERT model of the same dimensionality. Nothing failed
loudly; a specification and an implementation simply disagreed for weeks
while both were individually correct at the time they were written. We now
treat ``the specification names a model'' and ``the runtime can load that
model'' as separate claims requiring separate tests.

%% =====================================================================
\section{When You Should Not Use This}
\label{sec:limits}

\textbf{When the corpus has no natural types.} If everything is one skill,
discovery has nothing to enumerate, and you have paid a schema's authoring
cost for a flat store's behaviour. The typing argument is a claim about
domains with vocabulary, not about text in general.

\textbf{When nobody will author skills.} The type system is
human-authored by construction. That is exactly why the skill count stops
growing, and exactly why a team unwilling to write and curate skills gets
the cost without the benefit.

\textbf{When retrieval accuracy at the discovery step matters more than
tokens.} Description-only indexing gave up \pn{13} points of top-1 accuracy
in exchange for the token budget. For a corpus small enough to read whole,
that is a bad trade.

\textbf{When writes outrun a single writer.} One writer per tenant, serial
within a tenant and parallel across them. This suits a knowledge base and
does not suit an event firehose.

\textbf{When you need sub-millisecond retrieval.} An embedded HNSW index
inside a shared analytical file is not a dedicated vector service, and we
gave up measured latency for architectural simplicity on purpose.

\textbf{When your live sources are slow, or when you need to search them.}
A read that proxies a REST call inherits that call's tail latency, and live
data is never indexed, so a remote instance is reachable by link and not by
similarity. If either matters, materialise the source instead and accept the
staleness you were trying to avoid.

\textbf{When you need cross-tenant federation, or hard multi-tenant
isolation today.} See \expref{18}.

%% =====================================================================
\section{Related Work and Summary}

\textbf{Agent memory.} MemGPT~\cite{memgpt} pages context in and out of a
window, which is a mechanism for managing an untyped store rather than a
proposal for typing it. Mem0~\cite{mem0} and Zep~\cite{zep} extract entities
and relations into a graph automatically; the contrast with our position is
sharp and deliberate, since a machine-inferred schema is one nobody can read
in the morning, review in a pull request, or ship as a pack.
GraphRAG~\cite{graphrag} summarises at index time and answers global
questions well, but has no write loop: the index is built, not
maintained. A-Mem~\cite{amem} and the memory stream of generative
agents~\cite{genagents} both organise episodic memory without giving it a
type system a domain expert would recognise.

\textbf{Skills, and what they left out.} Agent Skills~\cite{agentskills}
introduced progressive disclosure for the procedural axis and stopped there.
Our claim is that the same mechanism extends to episodic memory through one
small generalisation, letting the instance declare its skill. In the domains
we work in, that extension covers most of the volume, and volume is what
sets the cost.

\textbf{Federated access.} Lakehouse catalogs~\cite{ducklake,iceberg} and
virtual query engines unify access to \emph{data} without unifying the
addressing of \emph{knowledge}: they give one query language over many
sources, where the corollary of Section~\ref{sec:corollary} gives one
referent space over many substrates, of which relational data is one.
Event sourcing~\cite{eventsourcing} is the direct ancestor of the second
duality; what is new here is that the projection function is an agent, and
its code is a document in the store it projects into.

\textbf{Summary.} A skill is a schema and a prompt; an instance is a row and
a document; an event is a fact and a callback is its projection; and because
the skill declares the backend, the substrate under an instance is a
property of its type. That last point is the one we underestimated when we
started, and the one we would keep if we had to discard the rest. Whether
the token invariant survives contact with a corpus a thousand times larger
than the one we have is the question we cannot answer yet, and
Section~\ref{sec:eval} says precisely which measurement would settle it.

\textbf{Availability.} The specification, the eleven architecture decision
records, the sixty-two discovered-problem notes cited throughout, and the
plan this paper was written against are in the project repository.

\balance
\bibliographystyle{ACM-Reference-Format}
\bibliography{refs}

\end{document}
