# Rust
/target/
**/*.rs.bk
# Library-only workspace today; revisit when first binary lands (planned PR 4).
# NOTE: gitignore does NOT support inline comments — `#` only starts a comment
# at column 0. Keep the comment on its own line above the pattern.
Cargo.lock

# IDE / editor
/.idea/
/.vscode/
*.swp
*.swo
.DS_Store

# Secrets / local config
.env
.env.*
!.env.example

# DuckDB scratch (evaluation harnesses, local runs)
*.duckdb
*.duckdb.wal
*.duckdb.tmp
*.db-shm
*.db-wal

# Doc build artefacts
/docs/build/
/docs/book/

# OS
Thumbs.db
