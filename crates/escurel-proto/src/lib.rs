//! Generated Protobuf + tonic stubs for the Escurel v1 gRPC
//! surface. The `.proto` file lives at `proto/escurel.proto`; the
//! generated code is compiled by `build.rs` into `OUT_DIR` and
//! re-exported below under the `v1` module so the package path
//! matches the proto `package escurel.v1`.

pub mod v1 {
    tonic::include_proto!("escurel.v1");
}
