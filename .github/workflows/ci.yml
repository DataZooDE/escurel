name: CI

on:
  push:
    branches: [main]
  pull_request:

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

env:
  CARGO_TERM_COLOR: always
  RUSTFLAGS: "-D warnings"
  RUST_BACKTRACE: short

jobs:
  ci:
    name: fmt + clippy + test + build
    runs-on: ubuntu-latest
    # Cold duckdb compile is ~15 min in debug and ~10 min in release
    # (LTO). With both stages running back-to-back on a fresh runner
    # the job needs >30 min headroom before the cache is primed; once
    # rust-cache restores libduckdb-sys both stages drop to <1 min.
    timeout-minutes: 60
    steps:
      - uses: actions/checkout@v4

      # rust-toolchain.toml drives the channel + components; rustup
      # honours it automatically on first invocation.
      - name: Install Rust toolchain
        run: |
          rustup show active-toolchain || rustup toolchain install
          rustup component add rustfmt clippy

      - uses: Swatinem/rust-cache@v2
        with:
          shared-key: workspace
          # Save the cache even when a later step fails — without
          # this, a single test failure throws away the duckdb
          # compile and every push re-pays the 15-min cold build.
          cache-on-failure: true

      - name: cargo fmt --check
        run: cargo fmt --all -- --check

      - name: cargo clippy
        run: cargo clippy --workspace --all-targets -- -D warnings

      - name: cargo test
        run: cargo test --workspace --all-targets

      - name: cargo build --release
        run: cargo build --workspace --release
